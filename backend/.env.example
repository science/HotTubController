# Hot Tub Controller Backend Environment Configuration
#
# DEPLOYMENT INSTRUCTIONS:
# 1. Copy the appropriate config/env.* file to this location (backend/.env)
# 2. Update any placeholder values (API keys, etc.)
# 3. The app always reads from backend/.env - no environment variables needed
#
# For quick setup, copy one of these:
#   cp config/env.development .env        # Local development (stub mode)
#   cp config/env.testing .env            # Running tests
#   cp config/env.staging.example .env    # Staging server (add your API key)
#   cp config/env.production.example .env # Production (add your API key)

# Application environment: development, testing, staging, or production
APP_ENV=development

# IFTTT mode: stub, live, or auto
# - stub: Always use simulated IFTTT calls (safe for testing)
# - live: Always use real IFTTT calls (requires IFTTT_WEBHOOK_KEY)
# - auto: Automatically choose based on environment and key availability
IFTTT_MODE=stub

# IFTTT webhook key (required for live mode)
# Get this from https://ifttt.com/maker_webhooks/settings
# IFTTT_WEBHOOK_KEY=your-webhook-key-here

# Authentication - Bootstrap Admin User
# IMPORTANT: Change these values for production deployments!
AUTH_ADMIN_USERNAME=admin
AUTH_ADMIN_PASSWORD=password
JWT_SECRET=change-this-to-a-secure-random-string
JWT_EXPIRY_HOURS=24
