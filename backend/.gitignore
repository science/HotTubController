/vendor/
/composer.phar
/.phpunit.cache/
/logs/*.log
/storage/logs/

# Environment files with secrets - NEVER commit these
.env
!.env.example

# Config files that may contain real keys - staging and production have real API keys
# (development and testing configs are safe to commit as they use stub mode)
config/env.staging
config/env.production

# User database - contains hashed passwords, should never be committed
/storage/users/
