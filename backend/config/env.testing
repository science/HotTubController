# Testing Environment Configuration
# Used automatically by PHPUnit tests

APP_ENV=testing

# IFTTT Configuration
# Always use stub mode for tests - never make real API calls
IFTTT_MODE=stub

# No API key needed for tests
# IFTTT_WEBHOOK_KEY=
