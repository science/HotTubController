# Development Simulation Environment Configuration
# SAFE MODE: Cannot trigger real hardware
APP_ENV=development
SIMULATION_MODE=true
DEBUG=true

# Debug Output Level (0=errors only, 1=warnings, 2=info, 3=debug)
DEBUG_LEVEL=1

# Logging
LOG_LEVEL=info
LOG_PATH=storage/logs/simulation.log

# Authentication (test hash for password "sim-master-password")
MASTER_PASSWORD_HASH='$2y$10$KI9zWOkmkFPuU0OO6TGOEe51BZJQzACAvPjD0Er4N09KjXc8BDbgG'

# CORS Settings (for frontend development)
CORS_ALLOWED_ORIGINS='["http://localhost:5173", "http://localhost:3000"]'
CORS_ALLOWED_METHODS='["GET", "POST", "PUT", "DELETE", "OPTIONS"]'

# Rate Limiting
RATE_LIMIT_REQUESTS_PER_MINUTE=60
RATE_LIMIT_REQUESTS_PER_HOUR=1000

# File Storage
TOKEN_STORAGE_PATH=storage/simulation_tokens.json

# Server Settings
SERVER_HOST=localhost
SERVER_PORT=8080

# External API Configuration - EMPTY FOR SAFETY
# These empty values force simulation mode in all API clients
WIRELESSTAG_OAUTH_TOKEN=
IFTTT_WEBHOOK_KEY=

# Device IDs (can be configured for simulation)
WIRELESSTAG_HOT_TUB_DEVICE_ID=sim_hot_tub_device
WIRELESSTAG_AMBIENT_DEVICE_ID=sim_ambient_device

# Simulation Configuration
SIMULATION_HEATING_RATE=0.5
SIMULATION_COOLING_RATE=0.014
SIMULATION_DEFAULT_TEMP=98.5
SIMULATION_DEFAULT_SCENARIO=idle

# Hot Tub Configuration
HOT_TUB_HEATING_RATE=0.5

# Storage Paths (isolated from production)
STORAGE_PATH=./storage/simulation